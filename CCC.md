# 奖励生成：用条件生成模型从团队回报学习个体奖励

---

## 摘要

在合作多智能体强化学习（MARL）中，环境只提供团队奖励——个体智能体的奖励来源是缺失的。现有方法均隐式处理该问题：值函数分解（需要结构假设）、反事实基线（需要默认动作，其语义在智能体异质时失效）、或均匀共享（无区分性）。本文提出**奖励生成**范式，围绕三个支柱展开：个体奖励由**条件贝叶斯流网络（BFN）**生成，其以联合状态-动作为条件输出逐智能体势的分布；奖励由采样的势与真实团队信号组合而成；整个系统以**基于生成奖励的强化学习**端到端优化——生成器由团队优势经 REINFORCE 训练，策略由 PPO 训练——环境无需提供任何个体贡献信息。范式的核心洞察是：一旦个体奖励被表示为**分布**（而非确定函数），团队回报即为充分训练信号；无需层级优化、监督标签或反事实评估。在 StarCraft 多智能体挑战（SMAC）上，本方法在异质智能体地图取得 93.8–100% 峰值胜率，在 Super Hard 地图 MMM2 上取得 84.4%。

**关键词**：多智能体强化学习、奖励生成、信用分配、贝叶斯流网络

---

## 1 引言

多智能体强化学习研究多个智能体如何通过与环境及彼此交互学习合作策略。一个长期困难是奖励结构：合作任务中环境对联合行为只返回**单一的团队奖励**，而每个智能体的策略是独立参数化、独立更新的。团队级信号如何驱动智能体级学习，就是信用分配问题；两大主流范式都在**隐式**处理它。

在值函数分解方法中，QMIX 是重要突破：个体效用经单调混合网络聚合为联合值，保证了个体贪心更新与联合贪心更新的一致性。QMIX 以其在 StarCraft 多智能体挑战（SMAC）上的高效与强表现广受认可。但该路线携带一个结构假设：混合族本身。当一个智能体的边际值非单调地依赖于队友状态时——治疗单位造成的正是这种情形——所假设的结构无法表示真实的个体-全局关系。此外，这些系统从不计算个体奖励；信用只是分解结构的副产品。

采用"集中训练、分散执行"（CTDE）的策略梯度方法，如 MADDPG 与 MAPPO，完全回避了分解。每个智能体从共享的团队奖励出发、借助中心化批评家训练。这使方法通用且可扩展，但信用分配完全留给了批评家：到达每个智能体的奖励是相同的，因此加权策略梯度的优势对所有智能体相同（定理 1(i)）。每个智能体的动作对团队结果的贡献未在其学习信号中体现；负担落在批评家与各智能体自身的得分函数上，且随队伍增大而加重。

第三条路线通过反事实显式处理信用分配。COMA 与差分奖励将每个智能体的贡献估计为 $D_i = R(\mathbf{a}) - R(c_i, \mathbf{a}_{-i})$，即智能体 $i$ 改取参照动作 $c_i$ 时团队结果的变化。该估计需要选取 $c_i$，而选取没有原则性依据。异质队伍中问题更尖锐：同一个"不动"对输出单位意味着损失输出，对治疗单位意味着队友死亡，因此 $D_i$ 的值跨单位类型不可比。

近期工作尝试从数据中**学习**个体奖励而非人工设计。TAR² 用 Transformer 对终局奖励在时间与智能体两个维度上重分配，通过对结果导出标签的监督回归训练，并在 episode 收集之后应用。Yang 等人以势函数参数化学习个体势，但其势是 state-only 的确定函数，训练涉及双层优化。QVPO 以 Q 加权变分目标训练扩散策略，证明了值信号可以引导生成模型，但作用于动作空间而非奖励空间。

综合来看，这些方法暴露出同一个缺口：没有一种方法回答那个更前置的问题——**个体奖励应当从哪里来、由什么决定、怎样学习？** 分解把问题藏进混合结构，共享奖励把它留给批评家，反事实用一个任意参照作答，学习式方法或依赖派生标签的监督、或依赖层级优化。

本文论证：一旦认真对待映射本身的性质，这个问题有干净的答案。从团队反馈到个体奖励的映射是**欠定的**：同一个联合结果可与多种归因一致，标量团队奖励无法裁决。欠定映射的正确表示是条件分布 $p(\mathbf{r}\mid s,\mathbf{a})$，而非点估计。而分布可以**采样**，样本有 **log-probability**——这正是一个策略梯度学习器所需要的。所以个体奖励应当被**生成**：从条件生成模型中采样，训练信号只有团队自身的回报。一旦奖励成为分布，层级优化、监督标签、反事实评估都不再需要。

我们将该思想实现为完整系统。一个条件贝叶斯流网络（BFN）将联合状态-动作映射为逐智能体势的分布；个体奖励由采样的势与真实团队信号按势差形式组合，使生成贡献沿轨迹伸缩、从而有界；一个在真实奖励上训练的团队批评家提供团队优势；生成器以 REINFORCE 依该优势更新。两个结果支撑这一设计：定理 1 表明共享奖励训练在结构上不可能产生逐智能体优势，而奖励生成可以；定理 2 表明该 REINFORCE 更新是分配目标的无偏估计，因此团队优势本身就是充分信号。

我们的贡献为：

1. **奖励生成范式**。个体奖励的来源问题被显式化并以生成作答：条件生成模型直接输出个体奖励，以势函数参数化保证时间一致性。不设个体-全局聚合的结构假设、不用默认动作、类型无关。
2. **基于 BFN 的奖励生成器**。个体奖励被建模为随机变量：其归因是不确定的（环境随机性带来偶然不确定性，归因未解决带来认知不确定性），因此生成器必须输出一个从无信息先验出发、随证据锐化的分布。BFN 恰好提供这种不确定性感知建模——在参数空间对奖励分布做迭代贝叶斯细化，其闭式高斯输出的 log-density 可被 REINFORCE 直接消费（§4.4.1）。
3. **基于生成奖励的强化学习优化**。生成器由团队优势经 REINFORCE 训练（我们证明这是无偏且充分的信号，定理 2）；策略以零修改的 PPO 在生成奖励上训练，奖励的逐智能体差异产生分化的优势（定理 1）。

本文其余部分组织如下：§2 预备知识；§3 相关工作；§4 方法及其理论性质；§5 实验；§6 结论。

---

## 2 预备知识

### 2.1 分散部分可观测马尔可夫决策过程

多智能体任务形式化为 Dec-POMDP $\mathcal{M} = \langle \mathcal{S}, \{\mathcal{A}_i\}_{i=1}^N, P, R, \{\Omega_i\}_{i=1}^N, O, \gamma \rangle$，其中：

- $\mathcal{S}$：全局状态集合
- $\mathcal{A}_i$：智能体 $i$ 的动作空间，联合动作 $\mathbf{a} = (a_1, \dots, a_N)$
- $P: \mathcal{S} \times \mathcal{A} \times \mathcal{S} \rightarrow [0,1]$：状态转移函数
- $R: \mathcal{S} \times \mathcal{A} \rightarrow \mathbb{R}$：团队奖励函数
- $\Omega_i$：智能体 $i$ 的局部观测空间，观测函数 $O_i: \mathcal{S} \to \Omega_i$
- $\gamma \in [0,1)$：折扣因子

每个智能体 $i$ 基于动作-观测历史 $\tau_i = (o_i^1, a_i^1, \dots, o_i^t, a_i^t)$ 构建随机策略 $\pi_i: \tau_i \rightarrow \Delta(\mathcal{A}_i)$。联合策略 $\boldsymbol{\pi} = (\pi_1, \dots, \pi_N)$ 最大化

$$J(\boldsymbol{\pi}) = \mathbb{E}_{\boldsymbol{\pi}}\Big[\sum_{t=0}^{\infty} \gamma^t R(s_t, \mathbf{a}_t)\Big].$$

### 2.2 势函数奖励重塑

奖励重塑通过附加塑造项 $F(s,s')$ 加速学习：$R'(s,a,s') = R(s,a,s') + F(s,s')$。Ng 等人证明：当 $F$ 取势差形式 $F(s,s') = \gamma\Phi(s') - \Phi(s)$ 时，最优策略保持不变。该保证来自伸缩论证：沿任意轨迹，附加奖励之和为 $\gamma^{T+1}\Phi(s_{T+1}) - \Phi(s_0)$——只依赖端点的边界项，因此轨迹的长期排序不变。Wiewiora 证明该形式等价于 Q 值初始化；Devlin 与 Kudenko 将不变性扩展到多智能体 Nash 均衡。本文采用势形式是为其伸缩性质；由于我们的势以联合动作为条件，state-only 的不变性保证既不适用也不需要。

### 2.3 贝叶斯流网络

贝叶斯流网络（BFN）是一类通过迭代贝叶斯更新细化分布参数的生成模型。它作用于 $D$ 维数据向量 $\mathbf{x}$，定义四个分布：**输入分布** $p_I(\mathbf{x}\mid\boldsymbol{\theta})$（当前信念，参数 $\boldsymbol{\theta}$）；**发送分布** $p_S(\mathbf{y}\mid\mathbf{x};\alpha)$（对数据加精度 $\alpha(t)$ 的噪声）；**输出分布** $p_O(\mathbf{x}\mid\boldsymbol{\theta},t)$（由变换当前参数的网络发出）；**接收分布** $p_R$（对输出施加同样噪声过程）。采样从无信息先验 $\boldsymbol{\theta}_0$ 出发，经 $K$ 步贝叶斯更新逐步细化参数：

$$\boldsymbol{\theta}_0 \rightarrow \boldsymbol{\theta}_1 \rightarrow \cdots \rightarrow \boldsymbol{\theta}_K.$$

每一步以贝叶斯规则将内部样本并入当前参数。与需要在数据上跑前向/反向过程的扩散模型不同，BFN 直接在分布参数上操作；对离散数据网络输入位于概率单纯形上、对连续数据位于矩空间中，这支持少步生成与稳定密度。本文的连续情形下，输出分布 $p_O$ 是高斯 $\mathcal N(\boldsymbol{\psi};\,\mu_\phi(h),\,I)$，其均值 $\mu_\phi(h)$ 由精炼后的参数 $\boldsymbol{\theta}_K$ 经网络读出（$h$ 为条件编码，见 §4.4）；采样即重参数化抽取 $\boldsymbol\psi=\mu_\phi(h)+\varepsilon$。我们以 BFN 作为奖励生成器；范式本身不依赖这一选择。

---

## 3 相关工作

### 3.1 值函数分解

VDN 将联合值可加分解，QMIX 单调混合个体效用，QTRAN 用约束损失放松单调性，Weighted QMIX 对被低估的联合动作降权，QPLEX 采用 duplex dueling 结构。这些方法中，每个智能体被记多少功劳由所选分解结构决定，信用分配退化为混合族的设计。可加性与单调性排除了"一个智能体的边际值非单调依赖队友状态"的耦合——治疗单位造成的正是这种情形。个体奖励在这些系统中从未被表示。我们的方法不做值分解：以团队回报为给定，直接在奖励空间学习团队到个体的映射。

### 3.2 共享奖励的集中训练

MADDPG 与 MAPPO 以中心化批评家从共享团队奖励训练分散策略。信用分配留给批评家——它必须从一个对所有智能体相同的信号中为个体动作归因价值。定理 1(i) 给出后果：共享奖励加共享批评家下，所有智能体得到相同优势，策略梯度除各智能体自身得分函数外不携带任何逐智能体信息。我们的方法中分化由生成器供给：$r_i$ 不同，普通 GAE 即产生逐智能体优势（定理 1(ii)），PPO 无需任何改动。

### 3.3 反事实与差分奖励

差分奖励、COMA 与 MACA 以参照动作度量贡献：$D_i = R(\mathbf{a}) - R(c_i, \mathbf{a}_{-i})$。参照 $c_i$ 是无原则依据的设计选择，且在异质队伍中其含义随单位类型而变：输出单位不动损失输出，治疗单位不动使队友死亡。所得 $D_i$ 跨类型不可比（§1.2(c)）。我们的生成器产生以 $(s, \mathbf{a})$ 为条件的**绝对分配**，不涉及任何参照动作。§5.3 直接比较两种信号：同一生成器、同一训练管线，反事实信号在 3s5z 上达到 9.4%，团队优势达到 93.8%。

### 3.4 学习式个体奖励

这是与我们最接近的方向。TAR² 用 Transformer 对终局奖励做时间与智能体维度的重分配；重分配通过对结果导出标签的监督回归训练，并在 episode 收集后应用。Yang 等人以与我们相同的 PBRS 参数化学习个体势，但其势是状态的确定函数，训练涉及双层优化。QVPO 以 Q 加权变分目标训练扩散策略，说明值信号可引导生成模型，但在动作空间进行。我们的生成器在表示与信号上都不同：个体奖励表示为条件分布，REINFORCE 直接适用，团队优势单独即为充分训练信号（定理 2）；不涉及监督标签或双层优化，且生成在线发生——在策略训练过程中，而非事后。

---

## 4 方法

### 4.1 总览

本方法建立在 CTDE actor-critic 框架上。与 MAPPO 相同，每个智能体持有一个由 PPO 更新的策略 $\pi_i$，训练时借助中心化的批评家；不同之处在于，进入策略更新的奖励不再是共享的 $R/N$，而是由一个**奖励生成器**逐智能体产生的 $r_i$。

核心思想一句话：**把奖励设计本身变成一个强化学习问题**。生成器输出的分配 $\boldsymbol\psi$ 是"动作"，团队优势 $A^{tot}$ 是"奖励"，生成器 $p_\phi$ 是"策略"，按 REINFORCE 更新。由此，多智能体信用分配问题被转化为分配空间上的标准策略学习。

本节组织：§4.2 指出共享奖励在 actor-critic 中的结构性限制，明确逐智能体奖励是唯一缺失、且只能从团队回报学出的成分；§4.3 给出建模假定——个体奖励是不确定的量，以条件分布刻画；§4.4 构造奖励生成器并给出奖励的组合形式；§4.5 给出三个学习器的完整训练流程；§4.6 理论性质。图 1 为系统结构。

### 4.2 MAPPO下信用分配的结构性限制

先回顾 MAPPO 型更新。每个智能体按策略梯度更新：

$$\nabla_{\theta_i} J = \mathbb{E}\big[\nabla_{\theta_i}\log\pi_i(a_i\mid\tau_i)\cdot \hat{A}_i\big], \qquad \hat{A}_i = \mathrm{GAE}_\lambda(r_i), \tag{4}$$

其中优势 $\hat A_i$ 由智能体自身的奖励序列 $r_i$ 经 GAE 估计。共享奖励设定取 $r_i = R/N$：奖励对所有智能体相同，共享的中心化批评家 $V$ 也相同，于是每个智能体的时间差分残差

$$\delta_{i,t} = \frac{R_t}{N} + \gamma V(s_{t+1}) - V(s_t)$$

不依赖 $i$，进而 $\hat A_i$ 对所有智能体**完全相同**（定理 1(i)）。也就是说：在式 (4) 中，加权每个智能体策略梯度的优势是同一个数，"哪个智能体的动作导致了团队结果"这一信息**不在学习信号里**，逐智能体的区分只能来自各自得分函数的差别。这正是信用分配在共享奖励下的结构性限制。

要引入逐智能体的信用，需要的就是逐智能体的奖励 $r_i$。但 $r_i$ 从未被观测：环境不提供个体奖励，也不存在个体贡献的标签或示教。因此 $r_i$ 只能**从团队回报中学出来**。这带来两个设计约束：其一，策略学习管线（PPO、CTDE）保持不变——我们替换的是奖励输入，不是学习算法；其二，学习 $r_i$ 所用的信息不超过轨迹数据与团队奖励 $R$。

### 4.3 建模假定：个体奖励是不确定的量

团队赢了一局，功劳属于谁？团队奖励无法裁决：把胜利归因于输出单位的伤害或治疗单位的续命，都与观测到的 $R$ 相容。并且在训练开始时，正确的归因方式是未知的——它只能随着经验积累逐渐明确。

因此我们做一个建模假定：**把个体奖励的分配当作不确定的量来学习**。具体地，以归一化分配向量 $\boldsymbol\psi\in\mathbb R^N$ 表示一次分配，学习条件分布

$$p_\phi(\boldsymbol\psi\mid s,\mathbf a),$$

而不是学一个确定的映射 $(s,\mathbf a)\mapsto\boldsymbol\psi$。这个假定有两层 RL 含义：

- **采样即探索**：每次从分布中采样一个分配并部署，等价于尝试一种归因假设；分布保持多样性，就保持了在归因假设上的探索。
- **训练即信念更新**：训练早期分布在多种归因上铺开（因为还没有证据裁决），随训练在团队反馈支持的归因上收紧——与策略随奖励改进是同一种学习动态。

分布表示还带来一个决定性的技术可能：$\boldsymbol\psi$ 是被采样的、具有密度 $p_\phi$ 的随机变量，因此 $\nabla_\phi\log p_\phi(\boldsymbol\psi\mid s,\mathbf a)$ 有定义——生成器可以用**策略梯度**训练（§4.5）。确定映射没有对数密度，无法这样做。

在此假定下，训练目标是使"以生成奖励训练出的策略"表现好：

$$\phi^* = \arg\max_\phi\; J\big(\boldsymbol\pi(\mathbf r_\phi)\big), \tag{7}$$

其中 $\boldsymbol\pi(\mathbf r_\phi)$ 是以 $\mathbf r\sim p_\phi$ 为奖励训练的策略。§4.5 用交替训练逼近这一双层目标。

### 4.4 奖励生成

#### 4.4.1 生成器：条件 BFN

生成器须满足三个由 §4.3 直接引出的要求：输出分布而非点；分布从无信息先验出发、随证据收紧；且由于每个环境步都要采样一次、其密度进入 §4.5 的训练信号，采样必须廉价、对数密度必须精确有界。

我们以条件贝叶斯流网络（BFN）实例化。条件为全局状态与 one-hot 联合动作的拼接：

$$x = [\,s;\ \mathrm{onehot}(\mathbf{a})\,]\in\mathbb{R}^{d_s + N|\mathcal{A}|}, \qquad h = E_\phi(x); \tag{8}$$

从无信息先验出发，经 $K$ 步贝叶斯更新细化分布参数：

$$\boldsymbol{\theta}_0 = \boldsymbol{\theta}_{\mathrm{prior}}, \qquad \boldsymbol{\theta}_{k} = \Psi_\phi\big(\boldsymbol{\theta}_{k-1},\, h,\, k/K\big), \quad k=1,\dots,K; \tag{9}$$

输出为高斯分布：

$$p_\phi(\boldsymbol{\psi}\mid s,\mathbf{a}) = \mathcal{N}\big(\boldsymbol{\psi};\, \boldsymbol{\mu}_\phi(\boldsymbol{\theta}_K),\, \mathbf{I}\big). \tag{10}$$

贝叶斯更新的序列 $\boldsymbol\theta_0\to\cdots\to\boldsymbol\theta_K$ 恰好实现"先验铺开 → 逐步收紧"的学习动态；闭式高斯使采样与对数密度使用同一分布律，每个被部署样本的 $\log p_\phi$ 精确有界；细化在参数空间进行，$K$ 步即可完成采样。条件 (8) 只含联合状态-动作，不含类型信息、不含参照动作——生成器对队伍构成不敏感。

#### 4.4.2 奖励组合

采样得到的分配需要变成进入式 (4) 的标量奖励，并且**不能破坏团队原本的优化目标**。直接把任意扰动加在 $R/N$ 上是不行的：逐步扰动沿轨迹折扣累积，会随回合长度漂移学习目标。我们采用势函数重塑（PBRS）的形式，其经典性质是沿轨迹伸缩、不改变长期回报排序：

$$\sum_{t=0}^{T}\gamma^t\big(\gamma\Phi(s_{t+1}) - \Phi(s_t)\big) = \gamma^{T+1}\Phi(s_{T+1}) - \Phi(s_0), \tag{11}$$

右端只依赖回合端点，与路径无关。

**定义 1（生成式个体奖励）** 给定 $\boldsymbol{\psi}_t\sim p_\phi(\cdot\mid s_t,\mathbf a_t)$（式 (10)）、幅度系数 $\kappa>0$、引入系数 $\beta_t\in[0,1]$，定义

$$r_i(t) = \frac{R_t}{N} + \beta_t\cdot\kappa\cdot\big(\gamma\,\psi_i(s_{t+1},\mathbf{a}_{t+1}) - \psi_i(s_t,\mathbf{a}_t)\big). \tag{12}$$

+熵   式 (12) 的构成：基础项 $R_t/N$ 保证真实团队信号始终在场——策略永远在真实信号上加训，而非在纯生成内容上加训；势差项 $\beta_t\kappa(\gamma\psi_i'-\psi_i)$ 携带全部逐智能体差异——两个智能体的奖励不同，当且仅当其势的演化不同；由式 (11)，该项沿任意轨迹的折扣累积以 $(1+\gamma^{T+1})\beta\kappa\|\boldsymbol\psi\|_\infty$ 为界、与回合长度无关，因此不会漂移团队的长期目标（注 4）。$\beta_t$ 从 $0$ 渐增至 $\beta_{\max}$：训练初期式 (12) 退化为共享奖励，待批评家给出有效的 $A^{tot}$ 后生成项再逐步介入；$\kappa$ 将生成项维持在真实奖励的量纲上。

**注 1**（不设守恒）不施加 $\sum_i r_i = R$；式 (11) 的有界性已给出所需的全部保证，刚性总量约束只会限制个体奖励的自由度。

**注 2**（与 PBRS 的关系）势以联合动作 $\mathbf a$ 为条件，超出 state-only PBRS 的设定；采用势差形式是为式 (11) 的伸缩性质，不依赖其策略不变性保证。

### 4.5 训练：三个学习器

现在只剩一个问题：生成器 $p_\phi$ 自己靠什么信号训练？个体标签不存在；用生成奖励训练的值函数再反过来给生成器导出目标，会让两个网络的训练互相污染。剩下的、也是理应剩下的信号只有一个：**团队回报本身**——经一个只看真实奖励的批评家换算成优势。系统共有三个学习器：

| 学习器             | 输入信号           | 更新规则           |
| --------------- | -------------- | -------------- |
| 团队批评家 $Q_{tot}$ | 真实奖励 $R$       | $n$-step TD 回归 |
| 奖励生成器 $p_\phi$  | 团队优势 $A^{tot}$ | REINFORCE      |
| 策略 $\pi_i$      | 生成奖励 $r_i$     | PPO（不变）        |

**（a）团队批评家.** 中心化值函数 $Q_{tot}(s,\mathbf a)$ 以真实奖励的 $n$-step 回报做 TD 回归：

$$\mathcal{L}_Q = \Big(Q_{tot}(s_t,\mathbf{a}_t) - \Big[\sum_{k=0}^{n-1}\gamma^k R_{t+k} + \gamma^n Q_{tot}^{-}(s_{t+n},\mathbf{a}_{t+n})\Big]\Big)^2, \tag{13}$$

$Q_{tot}^{-}$ 为目标网络。$\mathcal L_Q$ 只含真实 $R$、不含 $\phi$：批评家评估联合动作好坏所用的尺度，始终以环境真实回报为锚，不被生成器的输出污染（注 3）。

**（b）生成器：分配空间中的 REINFORCE.** 由批评家定义团队优势（批内归一化）：

$$A_t^{tot} = R_t + \gamma Q_{tot}(s_{t+1},\mathbf{a}_{t+1}) - Q_{tot}(s_t,\mathbf{a}_t), \tag{14}$$

生成器按

$$\nabla_\phi J = \mathbb{E}_{\boldsymbol{\psi}_t \sim p_\phi}\big[\nabla_\phi \log p_\phi(\boldsymbol{\psi}_t\mid s_t,\mathbf{a}_t) \cdot A_t^{tot}\big] \tag{15}$$

更新。这正是 §4.1 的落点：把 $\boldsymbol\psi$ 看作动作、$p_\phi$ 看作策略、$A_t^{tot}$ 看作奖励，式 (15) 就是分配空间里标准的 REINFORCE。其直观语义与任何策略梯度相同：**某次分配之后团队结果好于预期（$A^{tot}>0$），这次分配的概率被推高；差于预期则被压低。** 生成器不需要任何监督目标——它通过"尝试分配、观察团队结果"来学习归因，这正是 RL 的试错逻辑。以 $A^{tot}$ 直接替代分配优势所引入的差项与采样值无关，是期望梯度为零的基线，故式 (15) 无偏（定理 2）。

**（c）策略：PPO 不变.** $\hat A_i$ 由 GAE 在式 (12) 的 $r_i$ 上计算，代入标准裁剪目标：

$$\mathcal{L}^{\mathrm{CLIP}}(\theta_i) = \mathbb{E}\Big[\min\big(\rho_i(t)\,\hat{A}_i(t),\; \mathrm{clip}\big(\rho_i(t),\, 1{-}\epsilon,\, 1{+}\epsilon\big)\,\hat{A}_i(t)\big)\Big], \tag{16}$$

$\rho_i$ 为重要性比率。由于 $r_i$ 逐智能体不同，$\hat A_i$ 发生分化（定理 1(ii)）——生成器学到的信用分配，经由标准的优势加权得分函数直接进入每个智能体的策略梯度。**信用分配的全部变化都发生在奖励里，学习算法本身零修改。**

**（d）训练循环.** 每轮依次执行：策略以当前奖励采轨迹 → 式 (13) 更新批评家 → 式 (15) 更新生成器 → 式 (16) 更新策略（算法 1），循环往复。三个学习器在同一批数据上交替更新，即 §4.3 双层目标的交替逼近。该流程在实验中稳定，原因直观：每个学习器的训练目标都不依赖它自己的输出——策略学于 $r_i$、批评家学于 $R$、生成器学于 $A^{tot}$，三者各自的信号源彼此独立。

### 4.6 理论性质

**定理 1（优势分化）** (i) 共享奖励（$r_i=R/N$）加共享批评家下，对所有 $i,j,t$ 有 $\hat A_i(t)-\hat A_j(t)=0$；(ii) 奖励由式 (12) 给出、$\beta>0$ 且势非退化时，$\hat A_i-\hat A_j=\beta\kappa\cdot\mathrm{GAE}(\gamma\psi_i'-\psi_i-\gamma\psi_j+\psi_j)$ 一般非零。（证明见附录）

**意义**：(i) 即 §4.2 指出的结构性限制的形式化——共享奖励下逐智能体的优势分化**不可表示**；(ii) 说明奖励生成恰好补上这一缺失：分化幅度由 $\beta\kappa$ 控制，方向由生成器决定，而生成器由式 (15) 从团队反馈学出。

**定理 2（分配空间策略梯度）** 式 (15) 是分配目标 $J^{\mathrm{alloc}}(\phi)=\mathbb E_{(s,\mathbf a)}[V^{\mathrm{alloc}}]$（$V^{\mathrm{alloc}}=\mathbb E_{\boldsymbol\psi\sim p_\phi}[A^{tot}]$）的无偏策略梯度估计。（证明见附录）

**意义**：团队优势单独即为充分的训练信号——生成器不需要监督标签、基线网络或外层优化循环。

**注 3（无循环依赖）** 式 (13) 不含 $\phi$，故 $\partial A^{tot}/\partial\phi=0$：生成器的训练信号不经过其自身输出。

**注 4（聚合有界）** 对式 (12) 求和并应用式 (11)：$\big|\sum_t\gamma^t\sum_i r_i(t)-\sum_t\gamma^t R_t\big|\le(1+\gamma^{T+1})\,\beta\kappa\,\|\sum_i\psi_i\|_\infty$，与回合长度无关。

---

## 5 实验

### 5.1 设置

SMAC 基准，8 张地图，$N=2$–10，覆盖同质与异质队伍（异质含 2–3 种单位类型，含治疗型 Medivac）。基线为 MAPPO 两种配置：**官方代码库复现**（原始 TensorBoard 曲线，与本文相同步数预算）与**文献报告值**（逐地图调参、至多 10M 步、6 种子中位数）。另以 HAPPO 文献值为参考：5m_vs_6m 77.5%（10M）、3s5z 97.5%（10M）、MMM2 88.8%（20M）。本文实现：策略以 PPO 更新、奖励输入替换为生成器输出（式 (12)）；生成器为 BFN。所有结果为单种子；评测为 32 局评估胜率。

### 5.2 主结果

表 1 汇总 8 张地图（格式 peak / ml10，%），统一步数预算对齐：3m 1M，2s_vs_1sc 与 2s3z 2.5M（均提前收敛），其余 5M；MMM2 以 5M 节点计。图 2 为对应学习曲线。

| 地图         | $N$/类型    | 本文               | MAPPO 复现        | MAPPO 文献值   |
| ---------- | --------- | ---------------- | --------------- | ----------- |
| 3m         | 3 / 同质    | 100 / 93.1†      | 100 / 99.4      | 100 / 100   |
| 2s_vs_1sc  | 2 / 同质    | 100 / 99.1†      | 100 / 98.8      | 100 / 100   |
| 2c_vs_64zg | 2 / 同质    | **93.8 / 85.6**† | 84.4 / 75.9     | 100 / 98.4  |
| 5m_vs_6m   | 5 / 同质    | 87.5 / 76.9      | **93.8 / 83.4** | 88.3 / 87.5 |
| 2s3z       | 5 / 异质×2  | **100 / 99.1**   | —               | 100 / 100   |
| 3s5z       | 8 / 异质×2  | **93.8 / 80.3**  | —               | 96.9 / 96.9 |
| 1c3s5z     | 9 / 异质×3  | **100 / 96.2**   | —               | 96.9 / 100  |
| MMM2       | 10 / 异质×3 | 78.1 / 64.7‡     | 78.1 / 65.6     | 87.5 / 86.7 |

† 反事实信号驱动的生成器变体（§5.3：该信号同质图可用、异质图失效）。‡ MMM2 训练在 5.68M 因 SC2 崩溃终止、终止时仍在上升；崩溃前峰值 84.4。

三点观察：

**（a）异质地图。** 四张异质地图在统一预算内峰值 78.1–100%。2s3z 与 1c3s5z 达 100%（分别在 0.72M 与 2.64M），以远小于文献的预算匹配 MAPPO 报告值；3s5z 达 93.8%（文献 10M 报告 96.9%，HAPPO 10M 97.5%）。MMM2（Super Hard，10 智能体、3 种单位、含 Medivac）上，本文与 MAPPO 复现在 5M 节点峰值均为 78.1%——基本持平——而本文运行在 5.68M 崩溃时仍在上升（崩溃前峰值 84.4）；MAPPO 复现至 10M 达 90.6%，文献报告 87.5%。

**（b）样本效率。** 在双方都能解出的地图上，本文比 MAPPO 复现更早达到高胜率：3m 0.08M vs 0.16M 破 90%；2s3z 0.32M；1c3s5z 2.64M。2c_vs_64zg 上本文 2.72M 破 90% 而复现 5M 内从未（84.4 vs 93.8）。5m_vs_6m 上复现更快（2.00M vs 2.48M 破 80%）且 5M 处更高——这是复现领先的一张图。MMM2 双方上升节奏几乎相同（1.52M vs 1.60M 首破 50%）。

**（c）同质地图持平。** 简单同质图上两者均达 93.8–100%；生成项不损害最终性能。生成项幅度与真实信号量级匹配是稳定的必要实现条件：MMM2 上固定幅度与信号失配约两个数量级时训练陷入零胜死循环；将 $\kappa$ 绑定 $\overline{|R|}/N$ 恢复量级匹配后，同预算下崩溃前达 84.4%。

### 5.3 消融：驱动信号

同一生成器架构、同一策略训练管线，仅改生成器驱动信号（3s5z；8 智能体；Stalker + Zealot）：

| 驱动信号 | 3s5z 峰值 | 机制 |
|---|---|---|
| 反事实 $g_i = Q_{tot}(s,\mathbf{a}) - Q_{tot}(s,(c_i,\mathbf{a}_{-i}))$ | 9.4% | $c_i$ 语义在异质单位上失效 |
| **团队优势（式 (15)）** | **93.8%** | 无需个体参照，类型无关 |
| 共享奖励（MAPPO，文献） | ~100% | 任务可解；区分度恒为零（定理 1(i)） |

消融隔离的是**生成器的驱动信号**而非任务难度：同一生成器、同一管线，反事实信号使系统在异质图崩溃（9.4%），团队优势使其成功（93.8%）——这是 §1.2(c) 反事实结构缺陷的直接实验证据。与文献共享奖励可达 ~100% 并不矛盾（后者不经过生成器）。

### 5.4 生成器行为分析

（待补：生成器输出的多模态性、跨智能体区分度、训练中的演化。）

---

## 6 结论与局限

**结论**：个体奖励可以被生成。一旦表示为分布，团队回报经 REINFORCE 即为充分训练信号——无需层级优化、监督标签或反事实评估。在异质智能体场景中，奖励生成显著优于反事实驱动的同构方法。

**局限**：需要精细微操的探索死锁场景（kiting 类）未解决——生成器加速首胜之后的信用分配，不加速首胜的发现；结果为单种子。

**未来工作**：生成器不确定性作为探索信号；生成内在奖励与课程奖励；扩展至连续动作与混合动机。

---

## 参考文献

20 条（VDN, QMIX, WQMIX, QTRAN, QPLEX, COMA, 差分奖励, MADDPG, MAPPO, PBRS, Wiewiora, Devlin, BFN, TAR², Yang 2024, QVPO, Diffuser, SMAC, MACA, 策略梯度定理）——见 `paper/references.bib`。
